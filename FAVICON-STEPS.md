# Favicon fix (Lovable → GTA)

**Done:** The old Lovable `favicon.ico` was removed. The site now uses only `favicon.svg` (your GTA logo).

**To get Google to update:**
1. Deploy this change.
2. In [Google Search Console](https://search.google.com/search-console), use **URL Inspection** for `https://grandtouchauto.ae/` and click **Request indexing**.

Google may take a few days to a few weeks to refresh the favicon in search results.
